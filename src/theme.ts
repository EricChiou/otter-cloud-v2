import { createTheme } from '@mui/material';

const theme = createTheme({
  typography: {
    fontFamily: 'Noto Sans Mono, Noto Sans TC, Microsoft JhengHei, Helvetica, Arial, monospace, sans-serif',
  },
});

export default theme;
